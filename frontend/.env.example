# Backend API (must match backend PORT in backend/.env)
# Option A: direct requests from the browser (CORS must allow localhost:5173)
VITE_API_URL=https://fashion-store-5.onrender.com/api

# Option B: leave VITE_API_URL unset and use Vite proxy (vite.config.js)
# VITE_API_PROXY_TARGET=http://localhost:5001
